#include<iostream.h>
using namespace std;
void fact(int n)
{
for(i=1,i<=n;i++)
 res*=i;
 cout<<"Factorial="<<res;
}
int main()
{
 int n;
 cout<<"\nEnter any number=";
 cin>>n;
 fact(n);
 return 0;
 }
