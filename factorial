#include<iostream.h>
using namespace std;
void fact(int n)
{
int res;
for(i=1, i<=n, i++)    //loop
{
 res*=i;
 cout<<"Factorial = "<<res;
}
int main()
{
 int n;
 cout<<"\nEnter any number: "; 
 cin>>n;
 fact(n);      //function call
 getch();
 return 0;
}
